<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/icon_btn"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintTop_toTopOf="@id/icon_view"
        app:layout_constraintBottom_toBottomOf="@id/icon_view"
        app:layout_constraintLeft_toLeftOf="@id/icon_view"
        app:layout_constraintRight_toRightOf="@id/icon_view"/>

    <View android:id="@+id/icon_view"
        android:layout_width="38dp"
        android:layout_height="38dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:background="@drawable/rounded_ctgry_view"/>
</androidx.constraintlayout.widget.ConstraintLayout>